# ADR 0004: SSE for incremental query responses
SSE is sufficient for one-way query progress and answer delivery without adding bidirectional session complexity. It matches the platform scope after voice removal.
