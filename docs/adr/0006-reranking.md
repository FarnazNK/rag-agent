# ADR 0006: Rank fusion instead of model reranking
Reciprocal rank fusion provides stable improvements without adding another paid model dependency to the serving path. Model reranking stays a future option once latency and cost budgets justify it.
